import 'package:flutter/material.dart';

class AppColors {
  // 🌟 Brand Colors (ApnaMart identity)
  static const Color primaryYellow = Color(0xFFF7C600); // Brand yellow
  static const Color primaryGreen = Color(0xFFF57C00);  // Action green

  // 🎨 Backgrounds
  static const Color scaffoldBg = Color(0xFFF4F5F7);    // Soft app background
  static const Color cardBg = Color(0xFFFFFFFF);        // White cards
  static const Color imageBg = Color(0xFFF1F1F1);        // Product image bg

  // 🖋 Text colors
  static const Color textPrimary = Color(0xFFF57C00);   // Main text
  static const Color textSecondary = Color(0xFF6B6B6B); // Subtext
  static const Color textMuted = Color(0xFF9E9E9E);     // Hints

  // 🔘 Buttons & States
  static const Color buttonGreen = Color(0xFFF57C00);
  static const Color buttonYellow = Color(0xFFF7C600);
  static const Color disabled = Color(0xFFBDBDBD);

  // 🧱 Borders & Dividers
  static const Color borderLight = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);

  // ⚠️ Status colors
  static const Color success = Color(0xFFF57C00);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFF9A825);
}
